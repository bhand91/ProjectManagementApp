@page
@model ProjectManagementApp.Pages.ProjectMembers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProjectMember[0].Project.ProjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectMember[0].Project.DueDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectMember[0].Project.ProjectDes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProjectMember) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Project.ProjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Project.DueDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Project.ProjectDes)
            </td>

            <td>

            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
